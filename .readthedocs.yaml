version: 2

build:
  os: ubuntu-24.04
  commands:
    # Unpinned, like `prefix-dev/setup-pixi` in the workflows.
    - curl -fsSL https://pixi.sh/install.sh | bash
    - ~/.pixi/bin/pixi run docs-build
    - mkdir -p "$READTHEDOCS_OUTPUT" && cp -a site "$READTHEDOCS_OUTPUT/html"
