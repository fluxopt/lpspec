# The PyPSA parity gate. Every rung of mathspec's PyPSA corpus — checked out
# here at the tag pyproject.toml pins, so the corpus follows the language pin
# and nothing else — solves through PyPSA and through this tree's specsolve, and
# the runner compares them as deep as the engines allow: one objective and one
# set of prices across the fence everywhere, the two linopy models label for
# label where the test oracle (`tests/linopy_lane`) can build. A rung the oracle
# cannot build yet is stamped, not failed, so this job tightens by itself as the
# oracle hardens.
#
# The certificate the runner writes, differential/pypsa/references.json, is
# committed; the last step fails when a run of this tree would rewrite it, so
# a stale stamp is a red diff rather than a passing string.
name: PyPSA parity

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pypsa-parity-${{ github.ref }}
  cancel-in-progress: true

jobs:
  parity:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - name: the corpus is mathspec at the tag pyproject.toml pins
        id: pin
        run: echo "ref=$(grep -o 'mathspec@v[^"]*' pyproject.toml | cut -d@ -f2)" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v7
        with:
          repository: energy-models/mathspec
          ref: ${{ steps.pin.outputs.ref }}
          path: corpus
      - uses: prefix-dev/setup-pixi@v0.10.1
        with:
          run-install: false
      # `uv run` syncs this project with its `dev` group, which carries the
      # oracle's linopy.
      - name: every rung against pypsa, with this tree's specsolve
        run: >
          pixi exec -s uv uv run
          --with-editable ".[xarray]"
          --with "pypsa==1.3.0" --with "highspy==1.15.1" --with "polars>=1.30"
          python differential/pypsa/parity.py corpus
      - name: the committed certificate, projections and tables are what this tree produces
        run: git diff --exit-code -- differential/pypsa && test -z "$(git status --porcelain differential/pypsa)"
